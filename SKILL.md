---
name: zhiqi-photo-relic-editorial
description: "智企·纸上留影：把用户照片提炼为独立的纸感版画、抽象摄影记忆图或手机封面。支持中文模式介绍、构图选择与动画思路说明；默认只交生成图、无标题，不拼原图。"
---

# 智企·纸上留影

从照片提炼主体关系、光线、颜色和情绪，生成可独立使用的纸感版画。原图是创作参考，不是默认交付内容。适合建筑、城市、旅行、荷塘、人群与具有清晰轮廓的生活瞬间。

## 先区分用户是在了解还是在创作

- 问“你负责什么”“有哪些风格”“怎么用”：读取 [模式与使用说明](references/modes-and-usage.md)，先介绍能力，再分类介绍构图、视觉语言、笔触和动画思路，附可复制中文口令；不因介绍而调用生图。
- 要求生成：有照片就直接执行。用户明确的参数优先；未指定时采用独立纸感版画、留白布局、quiet ink、无标题、静态图。不要求用户先填写全部参数。
- 缺少可用照片时再索取照片。只询问会改变交付且无法合理推断的信息。

## 交付约定

1. 默认交付一张独立生成图，不展示、不附送原图，不做上下拼接、左右对比或双联画。生成提示词必须明确“原图仅作参考，成品不包含摄影区域”。
2. 只有用户明确要对比或拼图时才提供；若用户要两张分开的图，就分别交文件，不能拼成一个文件。历史示例的上下拼图不是当前默认模板。
3. 默认无标题、无水印、无说明文字。用户明确要求文字才添加；“不要标题”持续适用于当前作品后续版本。
4. 展示实际生成图，不只描述完成。单张合成图片不能说成可编辑分层文件；静态图不能说成动画已完成。
5. 用户改变参数后，要重新落实相应视觉决策，不可以只改口头说明、标题或文件名。只修改指定细节时，保持其他已认可特征。

## 创作流程

1. 查看源照片，提取 3–5 个事实：主体及其关系、主色、明暗方向、尺度/空间、情绪。不能只抠出孤立图标而丢掉主体关系。
2. 读取 [提示词指南](references/afterimage-editorial-prompt.md)。选择配方：交付形式 / 构图 / 视觉语言 / 笔触 / 文字 / motion seed。
3. 用户指定模式时，读取 [模式与使用说明](references/modes-and-usage.md)，把模式翻译成具体画面要求。自然语言中文与英文口令等效。
4. 使用可用的图像生成或编辑工具，以用户照片为参考。默认只生成一张静态成品。缺少工具时如实说明，不能用提示词冒充图片。
5. 查看成品并逐项核对：独立图、标题要求、主体关系、指定构图、抽象程度、配色与手机可读性。需要比较新旧版时，实际查看两版。
6. 明显不合格时，针对失败项收紧提示词，重做一次；仍未满足则说明具体缺口，不宣称通过。用户只要图片时，交付文字保持简短。

## 模式必须改变可观察的结果

- `social-cover` 是构图，不是滤镜。默认 9:16，主视觉占画面约 60–80% 宽，建立清楚的视觉中心，重要主体避开边缘；不得照搬小版画压在原图下方的旧拼图。依照片调整前中后景、留白位置与主体尺度。
- 与留白版切换时，至少落实两项可观察变化，如主体尺度与位置、留白分布、裁切、前后层次。不是要求任意改动素材，而是兑现布局差异。
- 视觉语言改变时，检查实际造型组织变化（块面、叠层、轮廓或光影），不能靠色调轻微变化冒充另一种处理。
- `ink-assembly` 是后续墨块依次出现的思路，不是另一种静态画风。只交静态时显示全部组装后的画面；安排可辨识的主体块、支持形和细节，不画箭头、编号、时间轴。只增加这一选项时静态可能接近原版，要解释而不是承诺明显画风变化。
- 真正动画、透明素材或独立图层需要另行制作与核验，不能从“看起来可分层”推断已经有这些交付。

## 审美约束

温暖米白纸、来源于照片的深墨与低饱和色、少量有意义的点色；用块面、负形、稀疏线条表达可识别的整体关系。保留主体身份与姿态，简化人物细节。避免普通风景插画式铺满、重水彩、装饰性几何、浓重做旧和商业海报口号。

默认独立图允许把照片抽象为版画。只有用户明确要求保留摄影区域时，该区域才必须保持真实，不重绘人物或发明场景；不能因“保护照片”而强行把原图加入成品。

## 来源

基于 wnby/photo-relic-editorial 的 MIT 授权改编。保留 LICENSE 与 references，见 [来源与改动](references/attribution.md)。原版示例展示历史美学，不覆盖本版交付约定。
